import React from "react";
import { Container } from "./style";
import { I<%= props.name %> } from "./types";

export const <%= props.name %> = (props: I<%= props.name %>) => {
  return (
    <Container>
      <%= props.name %>
    </Container>
  );
};
