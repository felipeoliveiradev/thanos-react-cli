import React from "react";
import { Container } from "./style";
import { I<%= props.name %> } from "./types";

export function <%= props.name %>(props: I<%= props.name %>) {
  return (
    <Container>
       created
    </Container>
  );
}
