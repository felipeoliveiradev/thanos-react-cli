import React, { Component } from "react";
import { Container } from "./style";
import { I<%= props.name %> } from "./types";

export class <%= props.name %> extends Component {
  render() {
    return (
      <Container>teste</Container>
    );
  }
}
